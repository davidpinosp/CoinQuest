//
//  Post.swift
//  CoinQuest
//
//  Created by Matias Cevallos on 2/21/21.
//  Copyright © 2021 Matias Cevallos. All rights reserved.
//

import Foundation
/*
struct Venue: Codable {
    let id: Int?
    let lat: Double?
    let lon: Double?
    let category: String?
    let name: String?
    let created_on: Double?
    let geolocation_degrees: String?

}
struct Loc: Codable {
    let  venue: Venue
}

let url = URL(string: "https://coinmap.org/api/v1/venues/")!
URLSession.shared.dataTask(with: url) { (data, _
    , _) in
    if let data = data{
        let Users = try? JSONDecoder().decode([Loc].self, from: data)
        
        print(location[0].Venue.lat)
    }
}.resume()
*/
